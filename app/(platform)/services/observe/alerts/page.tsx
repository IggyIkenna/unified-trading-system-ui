"use client"

// Observe > Alerts — renders the same alerts page content
// but within the Observe service layout (not trading layout)
import AlertsPage from "@/app/(platform)/services/trading/alerts/page"

export default function ObserveAlertsPage() {
  return <AlertsPage />
}
