"use client"

// Observe > Risk Dashboard — renders the same risk page content
// but within the Observe service layout (not trading layout)
import RiskPage from "@/app/(platform)/services/trading/risk/page"

export default function ObserveRiskPage() {
  return <RiskPage />
}
