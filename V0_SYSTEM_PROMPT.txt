UNIFIED TRADING SYSTEM UI — V0 Core Context

ONE platform. THREE tiers (public/client/internal). SHARDS: CEFI/DeFi/Sports/TradFi. Workflow: Design→Simulate→Promote→Run→Monitor→Explain→Reconcile.

RULES (violation = wasted work):
1. NO V2/Refactored. Ask before breaking changes. Update UI_STRUCTURE_MANIFEST.json.
2. Shared components (filters/tables/headers) in shared/, not per-service. One impl, everywhere.
3. Zero hardcoding. API endpoints, colors, strings from shared/config/. Change once = everywhere.
4. Mocking: shared/mocks/ only (same in dev & tests). No per-page mocks.
5. Auth: useAuth() hook in shared/. One place for permission rules.
6. Shard-aware: scope data shard→venue→instrument. Never cross-shard.
7. If unable to follow rules, tell user immediately they will use a better agent.

BEFORE CODING:
1. ARCHITECTURE.md (vision) 2. CONFIG_REFERENCE.md (fields?)
3. SHARDING_DIMENSIONS.md (partition?)
4. API_FRONTEND_GAPS.md (ready? 🟢/🟡/🔴)
5. UI_STRUCTURE_MANIFEST.json (state?)
6. .cursorrules

REFACTOR vs NEW:
— Refactor? Read manifest. Ask "Approve?". Mark target. Update in place.
— New? Create services/{domain}/pages/{name}/. Add route in routing.tsx.

CODE PATTERNS:
— API: import API_CONFIG from 'shared/config/api'
— Branding: import COLORS, LOGOS from 'shared/config/branding'
— Shard: shard → venue → instrument (no cross-shard in one component)

STRUCTURE:
services/{domain}/ → pages/, components/, hooks/, types/
shared/ → components/, config/, hooks/, mocks/

QA GATE (Before PR/Deploy):
bash .scripts/verify.sh → clean → type-check → build → test
Catches broken imports, TypeScript, missing deps. See QA_GATES.md.

RESOURCES:
• config-registry.json = 2000+ fields
• .cursorrules = Rules + patterns
• REFACTORING_GUIDE.md = protocol
• UI_STRUCTURE_MANIFEST.json = tracker
• QA_GATES.md = checks

QUALITY STANDARD:
100% institutional standard or stop. No half-baked work. User escalates to better model.

MANDATE: Don't guess. Look it up. Shared. Centralized. One truth. Ask before breaking.
