UNIFIED TRADING SYSTEM UI — V0 Core Context

ONE platform. THREE tiers (public/client/internal). SHARDS: CEFI/DeFi/Sports/TradFi. Workflow: Design→Simulate→Promote→Run→Monitor→Explain→Reconcile.

RULES (violation = wasted work):
1. NO V2/Refactored. Ask before breaking changes. Update UI_STRUCTURE_MANIFEST.json.
2. Shared components (filters/tables/headers) in shared/, not per-service. One impl, everywhere.
3. Zero hardcoding. API endpoints, colors, strings from shared/config/. Change once = everywhere.
4. Mocking: shared/mocks/ only (same in dev & tests). No per-page mocks.
5. Auth: useAuth() hook in shared/. One place for permission rules.
6. Shard-aware: scope data shard→venue→instrument. Never cross-shard.

BEFORE CODING:
1. ARCHITECTURE_AND_WORKFLOW_OVERVIEW.md (vision)
2. context/CONFIG_REFERENCE.md (configurable?)
3. context/SHARDING_DIMENSIONS.md (data partition)
4. context/API_FRONTEND_GAPS.md (ready? 🟢/🟡/🔴)
5. UI_STRUCTURE_MANIFEST.json (state?)
6. .cursorrules or CODING_RULES_QUICK_START.md

REFACTOR vs NEW:
— Refactoring? Read manifest. Ask "Approve?". Mark target_structure. Update in place.
— New feature? Create services/{domain}/pages/{name}/. Add route to routing.tsx.

CODE PATTERNS (Never Hardcode):
— API: import API_CONFIG from 'shared/config/api'
— Branding: import COLORS, LOGOS from 'shared/config/branding'
— Shard: shard → venue → instrument. Never mix shards in one component.

STRUCTURE:
services/{domain}/ → pages/, components/, hooks/, types/
shared/ → components/, config/, hooks/, mocks/, types/

RESOURCES:
• config-registry.json = 2000+ fields (lookup, don't hardcode)
• .cursorrules = Rules 1-9 + patterns
• REFACTORING_GUIDE.md = protocol
• UI_STRUCTURE_MANIFEST.json = state tracker

MANDATE: Don't guess. Look it up. Shared components. Centralized config. One truth. Ask before breaking.
