UNIFIED TRADING SYSTEM UI — Next.js 16 App Router

⚠️ CODE ≠ RULES YET. app/ is flat, mocks are inline. Rules describe TARGET.
Read UI_STRUCTURE_MANIFEST.json first (current state + target + migration manifest).

ONE platform. Data-driven scoping. Internal/client/prospect see SAME pages, different data.
Never build separate client vs internal versions. API scopes by org + entitlements.

USPs: (1) 33 venues, 5 asset classes, full trading system access (2) seamless internal/external — same system, filtered (3) backtest↔live diff if using our data/models/strategies.

RULES:
1. No V2/Refactored. Update in place. Ask before breaking. Update UI_STRUCTURE_MANIFEST.json.
2. One page per view. PositionsTable, not ClientPositionsTable + InternalPositionsTable.
3. Zero hardcoding. Everything from lib/config/. Colors from globals.css tokens.
4. Mocking: migrating lib/*.ts inline → lib/mocks/ MSW. Dimensional: same endpoint, different data per persona. See REFACTORING_PLAN §2.4 migration manifest.
5. Auth: hooks/use-auth.ts → { role, org, entitlements }. Internal=["*"]. Client=subset.
6. Shard-aware: shard→venue→instrument. Market data=shared. Positions/risk=client-scoped. Registry=subscription-filtered. See context/SHARDING_DIMENSIONS.md.
7. State: Zustand (lib/stores/, every store has reset()). React Query (hooks/api/). URL params for filters.
8. Can't follow rules? Tell user immediately.

TARGET STRUCTURE (now implemented):
app/(public)/      marketing, login, signup, services/[domain]
app/(platform)/    THE product (internal+client):
  /overview          Service hub (grid, activity, quick actions, health)
  /service/[key]     Per-service subscription page
  /data              Data service (was /portal/data)
  /trading/          Live trading: positions, risk, alerts, markets (nested)
  /research/         Research: strategy/*, ml/*, execution/* (nested)
  /execution         Live execution analytics
  /reports/          Reports + executive (nested)
  /health            Service health
app/(ops)/         admin, devops, ops, manage/*, compliance, config, engagement, internal
components/        ui/, trading/, shell/, dashboards/
lib/config/        API, branding       lib/mocks/         MSW handlers + fixtures
lib/stores/        Zustand stores      lib/types/         Generated from openapi.json
lib/registry/      OpenAPI, configs    hooks/api/         React Query per-service

BEFORE CODING: UI_STRUCTURE_MANIFEST.json → .cursorrules → REFACTORING_PLAN_PHASE_1-4.md → ARCHITECTURE_AND_WORKFLOW_OVERVIEW.md → context/SHARDING_DIMENSIONS.md → context/CONFIG_REFERENCE.md → context/API_FRONTEND_GAPS.md

QA: pnpm lint && pnpm tsc --noEmit && pnpm build && pnpm test (70% coverage)

All resources inside this repo. No external references. lib/registry/ has fresh OpenAPI (298 endpoints), config-registry (46 configs), sharding_config, system-topology, ui-reference-data.

Institutional grade. Premium dark. Don't guess — look it up. One platform, not three.
