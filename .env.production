# Production environment variables
# Staging auth is set via Cloud Run env vars, NOT here
# NEXT_PUBLIC_STAGING_AUTH is intentionally NOT set here — it's injected at deploy time

# Mock API — always true for this demo deployment
NEXT_PUBLIC_MOCK_API=true
