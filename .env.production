# Staging auth — set to "true" to enable password gate on deployed site
# Uses NEXT_PUBLIC_ prefix so it's available client-side
NEXT_PUBLIC_STAGING_AUTH=true

# Mock API — always true for this demo deployment
NEXT_PUBLIC_MOCK_API=true
