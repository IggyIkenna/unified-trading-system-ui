# Module M — Naming Conventions & Consistency Audit

**Output:** `docs/UI-shared-components-Issues/13-NAMING-AUDIT.md`

## Search Patterns

1. Component naming: PascalCase consistently?
2. File naming: kebab-case for files, PascalCase for components?
3. Hook naming: `use-*` prefix?
4. Type/interface naming: consistent pattern (e.g., `*Props`, `*State`)?
5. Constant naming: `UPPER_SNAKE_CASE` for constants?
6. Event handler naming: `on*` / `handle*` consistently?
7. Boolean prop naming: `is*`, `has*`, `should*`?
8. CSS class naming in globals: consistent BEM or utility pattern?
