# Mock-static UI evaluation — repo operator brief

**For browser-only agents (no code access):** use the full handbook — every strategy, dependencies, expectations, and per-strategy UI checks are there:

**[`MOCK_STATIC_BROWSER_AGENT_HANDBOOK.md`](./MOCK_STATIC_BROWSER_AGENT_HANDBOOK.md)** (extended Codex-aligned edition: instruments, features, PnL, risk, latency, subscriptions, margin, UI — self-contained, no repo access).

**Repeatable full-platform Tier 0 checklist (env, five-track audit, Playwright, backlog):** [`END_TO_END_STATIC_TIER_ZERO_TESTING.md`](./END_TO_END_STATIC_TIER_ZERO_TESTING.md).

Give them **that file** (or a PDF export) **plus** the preview URL. They do not need this shorter spec.

---

## What this file is for

Maintainers cloning the UI repo: quick **env**, **personas**, and **URL sweep** without opening the handbook.

### Mock mode

| Item      | Detail                                                                                                 |
| --------- | ------------------------------------------------------------------------------------------------------ |
| Flag      | `NEXT_PUBLIC_MOCK_API=true` (baked in at **Next.js build** for static deploys)                         |
| Mechanism | Client `installMockHandler()` intercepts `/api/*` (see `lib/providers.tsx`, `lib/api/mock-handler.ts`) |
| Fixtures  | `lib/trading-data.ts`, `lib/*-mock-data.ts`, `lib/strategy-registry.ts`                                |

### Run locally

```bash
# .env.local
NEXT_PUBLIC_MOCK_API=true
pnpm dev
```

### Personas

`lib/auth/personas.ts` — exercise admin, internal-trader, client-full, client-data-only, client-premium.

### Minimum URLs

`/dashboard`, `/service/overview`, `/strategies`, `/strategies/[id]`, `/service/data/overview`, `/service/trading/overview`, `/service/trading/risk`, `/service/execution/overview`, `/service/research/overview`, `/service/reports/overview`.

### Sync rule

When **Codex** `unified-trading-codex/09-strategy/README.md` or per-strategy docs change, update **`MOCK_STATIC_BROWSER_AGENT_HANDBOOK.md`** (browser SSOT) and mock fixtures in `lib/`.

**PM index (machine vs hand-maintained):** `unified-trading-pm/docs/ui-alignment-ssot.md` — `ui-reference-data.json` and related generators are listed there; the browser handbook is **not** generated by those scripts.
