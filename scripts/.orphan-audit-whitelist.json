{
  "version": 1,
  "_comment": "Routes that are NOT and CANNOT BE human-navigable from any nav surface. Reviewers rejecting a whitelist PR MUST apply the triage test: 'Can a human user (any persona) gain from seeing this page?' If yes, it does NOT belong here. Every entry's reason MUST begin with one of MACHINE-ONLY / API-HANDLER / UNAUTHENTICATED-FUNNEL. SSOT: unified-trading-pm/codex/06-coding-standards/orphan-audit.md §Whitelist Triage Rule.",
  "entries": [
    {
      "path": "/health",
      "reason": "MACHINE-ONLY — liveness probe consumed by k8s / Cloud Run health checks; returns JSON, never user-navigated."
    },
    {
      "path": "/login",
      "reason": "UNAUTHENTICATED-FUNNEL — OAuth entry; reached via require-auth middleware redirect, not a declared tile."
    },
    {
      "path": "/signup",
      "reason": "UNAUTHENTICATED-FUNNEL — public signup flow; user cannot act until OAuth / invite redirect completes."
    },
    {
      "path": "/pending",
      "reason": "UNAUTHENTICATED-FUNNEL — post-signup holding page; reached via OAuth redirect flow before session established."
    },
    {
      "path": "/api/accounts/transfer-history",
      "reason": "API-HANDLER — Next.js route.ts fetched programmatically by the UI; not a page."
    },
    {
      "path": "/api/onboarding/download",
      "reason": "API-HANDLER — Next.js route.ts triggered by download button via fetch; not a page."
    },
    {
      "path": "/api/onboarding/upload",
      "reason": "API-HANDLER — Next.js route.ts accepting POST uploads from onboarding form; not a page."
    },
    {
      "path": "/api/onboarding/docs/list",
      "reason": "API-HANDLER — Next.js route.ts returning onboarding doc inventory for /admin/organizations/[id] Documents panel; fetched via query-string URL so the scanner's path-literal match misses it."
    },
    {
      "path": "/api/onboarding/docs/delete",
      "reason": "API-HANDLER — Next.js route.ts accepting per-doc admin deletes with a typed-confirm token; fetched from /admin/organizations/[id] Documents panel, never navigated."
    }
  ]
}
