# Deployment Service — Configuration

## Config Class

DeploymentConfig (from deployment_config.py). Key fields: project_id, region, config_dir.

## Config Location

configs/ directory. runtime-topology.yaml is SSOT for service topology.

## Environment Variables

- GCP_PROJECT_ID: Required
- Config paths via config_dir
