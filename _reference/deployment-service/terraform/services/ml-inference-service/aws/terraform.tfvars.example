# Example terraform.tfvars for ml-inference-service on AWS
# Copy to terraform.tfvars and fill in your values

# AWS Region
region = "ap-northeast-1"

# Job Configuration
job_name     = "ml-inference-service"
docker_image = "123456789012.dkr.ecr.ap-northeast-1.amazonaws.com/ml-inference-service:latest"

vcpus           = "4"
memory_mb       = "8192"
timeout_seconds = 3600
max_retries     = 3
environment     = "prod"

# IAM Roles (from shared-infrastructure module)
execution_role_arn = "arn:aws:iam::123456789012:role/trading-execution-role"
job_role_arn       = "arn:aws:iam::123456789012:role/trading-job-role"

# Networking (from shared-infrastructure module)
subnet_ids         = ["subnet-abc123", "subnet-def456"]
security_group_ids = ["sg-abc123"]
job_queue_arn      = "arn:aws:batch:ap-northeast-1:123456789012:job-queue/trading-queue"

# S3 Buckets
ml_models_bucket      = "ml-models-store"
ml_predictions_bucket = "ml-predictions-store"
features_bucket       = "features-delta-one-cefi"

# Workflow Configuration
workflow_name = "ml-inference-service-daily"

# Scheduler Configuration
schedule  = "0 13 * * *"  # 1:00 PM UTC daily
time_zone = "UTC"
