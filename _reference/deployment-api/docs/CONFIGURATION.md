# Deployment API — Configuration

## Config

Settings from deployment_api.settings. Key: GCP_PROJECT_ID, STATE_BUCKET, REDIS_URL, DEPLOYMENT_ENV.

## Config Source

Configs loaded from deployment-service via path dependency. configs/ symlinks to deployment-service/configs/.
