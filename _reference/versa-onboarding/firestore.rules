rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through server-side API routes (Firebase Admin SDK).
    // Client-side Firestore access is blocked.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
