"use client";

/** Archived ML layout — sub-tabs removed from ServiceTabs; pass-through only. */
export default function MLSectionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
