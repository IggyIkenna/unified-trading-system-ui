[x] On the top left side under the Odum logo, there's a status if the Azure API is reachable or not and which mode or which environment we are using. Dev, Prod, Mock mode. So insert, so the change its placement from the top left under the logo to the top right on the first row, the header row, left, just left of the bell icon and like in the middle of the bell icon and the search bar.

[x] fix venue coverage category filters not working (case mismatch between mock data and filter values)

[x] add sports as its own category separate from prediction_market, with api-football and footystats as venues, mock instruments and catalogue entries for both tabs

[x] make venue coverage filter buttons data-driven from DATA_CATEGORY_LABELS instead of hardcoded list

[x] build venue list client-side from instruments array instead of relying on mock handler's extra venues field

[x] align venue coverage filter UX with instrument catalogue — same per-category colors, toggle behavior, variant switching, and fix hover background bleed on active filters

[x] all top headers in row 1 that shows acquire, build, ..., report this lifecycle tabs should be locked at top, so that when i scroll down they does not hide and are always visible

[] at the top right corner, the All Strategies dropdown menu is showing all the strategies, please make them collapsible across different categories and also expand the height of the dropdown to be dynamic so that the strategies does not flow outside the view, and use 35% height as the max height of the dropdown

news in trading
corporate actions in features-calender
data type and leagues in data

[] in http://localhost:3100/services/data/coverage,
currently the coverage matrix is showing coverage across all the data points whether it is related to raw data or processed data or features and ml pipelines.
the data service is responsible for gathering data and processing it
so instruments data (missing from coverage), raw data ETL and processing the raw data - only this should be covered in the acquire tab.

everything related to strategies, ML models, execution, backtesting strategies/execution should be moved to Build lifecycle tab.

it should show the coverage for data only so all the columns after 1d that includes features, signals, ..., P&L should be migrated to other pages.
acrquire tab on top covers everything related to data, so venues ticks trades, book ,etc should stay on this page.
the second tab build should show a similar matrix but for features, ml models, signals, strategies, execution, backtests, etc.

## 404

http://localhost:3100/services/execution/overview
http://localhost:3100/services/execution/tca

## Updates to Mock data

### instrument registry

sports and predictions are seperated into individual category

- venues:
  prediction - polymarket, kalshi
  sports - api-football, footystats
